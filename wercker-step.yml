name: android-deploy-notify
version: 0.0.1
description: Deploy an APK to S3 after a successful build and notify multiple users/channels on slack.
keywords:
  - slack, android, s3
properties:
  notify_success:
    type: string
    required: false
  notify_fail:
    type: string
    return: false
  icon_url:
    type: string
    required: false
